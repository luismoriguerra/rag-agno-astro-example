services:
  postgres:
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: agentos_chat
    volumes:
      - agentos_chat_pg:/var/lib/postgresql/data

volumes:
  agentos_chat_pg:
